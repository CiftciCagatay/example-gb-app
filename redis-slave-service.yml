apiVersion: v1
kind: Service
metadata:
  name: redis-slave-service
  labels:
    name: redis-slave-service
spec:
  ports:
  - port: 6379
    targetPort: 6379
  selector:
    name: redis-slave
    tier: backend
